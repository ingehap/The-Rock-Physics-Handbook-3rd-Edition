# Project notes for Claude

- Anything that needs the repository owner's attention or a decision from them
  (license questions, ambiguous scope, blocked work) goes into
  `NEED_ATTENTION.md` at the repo root — add it there rather than only
  mentioning it in chat. Move resolved items to that file's "Resolved" section
  with their outcome.
- The Python port of `RPHtools/` is planned in `PORTING_PLAN.md`; the
  implementation lives in `pyRPHtools/` (package name `rphtools`).
